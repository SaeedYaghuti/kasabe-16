export interface Account {
    id?: number;
    title: string;
    titleArb: string;
    titlePer: string;
    description: string;
    // children: Account[];
    // parent: Account;
}

// parameters: { id, title, titleArb, titlePer, description, children,  parent}